apiVersion: v1
kind: Secret
metadata:
  name: house-control-secret
  namespace: house-control
  labels:
    app: house-control-container
    component: application
    environment: development
type: Opaque
data:
  # Database credentials
  SPRING_DATASOURCE_USERNAME: c21hcnRjaXR5  # smartcity (base64 encoded)
  SPRING_DATASOURCE_PASSWORD: c21hcnRjaXR5MTIz  # smartcity123 (base64 encoded)

  # Redis credentials
  SPRING_REDIS_PASSWORD: cmVkaXMxMjM=  # redis123 (base64 encoded)

  # RabbitMQ credentials
  RABBITMQ_USERNAME: YWRtaW4=  # admin (base64 encoded)
  RABBITMQ_PASSWORD: YWRtaW4xMjM=  # admin123 (base64 encoded)

  # Keycloak credentials (if needed)
  KEYCLOAK_CLIENT_SECRET: aG91c2UtY29udHJvbC1zZWNyZXQ=  # house-control-secret (base64 encoded)

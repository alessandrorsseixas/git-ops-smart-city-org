apiVersion: v1
kind: Secret
metadata:
  name: house-control-secret
  namespace: smart
  labels:
    app: house-control-container
    component: application
    environment: development
type: Opaque
data:
  # Database credentials
  SPRING_DATASOURCE_USERNAME: cG9zdGdyZXM=  # postgres (base64 encoded)
  SPRING_DATASOURCE_PASSWORD: cG9zdGdyZXM=  # postgres (base64 encoded)

  # RabbitMQ credentials
  RABBITMQ_USERNAME: Z3Vlc3Q=  # guest (base64 encoded)
  RABBITMQ_PASSWORD: Z3Vlc3Q=  # guest (base64 encoded)

  # Keycloak credentials (if needed)
  KEYCLOAK_CLIENT_SECRET: aG91c2UtY29udHJvbC1zZWNyZXQ=  # house-control-secret (base64 encoded)

apiVersion: v1
kind: Service
metadata:
  name: house-control
  namespace: smart-city
  labels:
    app: house-control-container
    component: application
    environment: development
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: house-control-container
  type: ClusterIP
